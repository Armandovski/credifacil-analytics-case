# Diretório de entrada com os CSVs (no Docker, é montado em /app/input)
INPUT_DIR=./input

# Raiz do armazenamento Medallion (local)
DATA_DIR=./data

# O dbt lê BRONZE_DIR para localizar os Parquets (globbing)
BRONZE_DIR=./data/bronze

# Arquivo do "warehouse" local (DuckDB)
DUCKDB_PATH=./data/warehouse.duckdb

# Data de corte ("as of") para cálculo de atraso/inadimplência (YYYY-MM-DD).
# Se vazio, usa a data de hoje.
AS_OF_DATE=2025-12-15

# Diretório de saída para exportações (opcional)
OUTPUT_DIR=./output
